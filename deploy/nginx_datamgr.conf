# Nginx 配置示例
# 请将此文件内容复制到您的服务器 Nginx 配置目录中 (通常是 /etc/nginx/sites-available/datamgr.it.com)

# 1. 当前社交平台 (运行在 8080 端口)
server {
    listen 80;
    server_name datamgr.it.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态文件缓存 (可选)
    location /static {
        alias /path/to/your/project/static;
        expires 30d;
    }
}

# 2. 另一个平台 (如果需要共存，必须运行在不同端口，例如 8081)
# server {
#     listen 80;
#     server_name other-domain.com;
#
#     location / {
#         proxy_pass http://127.0.0.1:8081; # 注意这里是不同的端口
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
